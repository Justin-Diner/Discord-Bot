/*  // Ping and Pong Response
 export const pingPong = client.on('messageCreate', msg => { 
        if (msg.content === prefix + 'ping') {
          msg.reply('pong')
 }
 })


import { client } from '../app.js' */